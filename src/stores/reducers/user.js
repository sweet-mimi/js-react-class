const initState = {
    id: '',
    role: '',
    name: '',
    avatar: '',
    isLogin: true
}

export default (state = initState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}